"""LinearSensor component.

The current sensor path is the PIO-SENT dashboard under ``pico_viewer/`` (see
``pico_viewer/README.md`` and ``wiki/topology.md``). The older ASCII ``F/G/A``
USB-serial driver and the ``lift_monitor`` GUI were removed in the fall-2026
cleanup (recoverable from the ``pre-cleanup`` git tag).
"""
