# tests

Scaffold for unit tests and firmware sketches. The previous test suite (old
LX3302A ASCII-serial calibration/monitoring scripts, ESP32 lift-mimic sketches,
legacy pellet-dispenser tests) was **cleared in the fall-2026 cleanup** as
outdated — recoverable from the `pre-cleanup` git tag if any is needed as a
reference.

Repopulate with tests for the current SENT sensor path and TMC2209 pump as they
are written.
